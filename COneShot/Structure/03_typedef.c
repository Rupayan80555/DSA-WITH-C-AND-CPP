#include<stdio.h>
// typedef int i;    // by using typedef we can rename any identifier's name according to our choice, we  also can use the previous name also.c  
int main(){
    int d=5;
    printf("%d",d);
}